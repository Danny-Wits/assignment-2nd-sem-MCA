#include <iostream>
using namespace std;

void quicksort(int *array, int size, int low, int mid, int high)
{
}

int helper(int *array, int size, int low, int high)
{
    if (high < low)
        return;
    int pivot = array[low];
}
int main()
{

    return 0;
}